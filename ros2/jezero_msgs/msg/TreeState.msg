# Behaviour-tree snapshot for the console (py_trees executive).
string ascii
string active_path             # e.g. Sol / Execute / Drive
string status                  # RUNNING | SUCCESS | FAILURE
bool safety_halt
string halt_reason
